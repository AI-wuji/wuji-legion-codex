# 第二师（视觉） - PPT/HTML高质生产线 + image2视觉补给 + 漫画流水线

## 核心理念：视觉生产线，不是工具拼盘

```
PPT制作 = GPT文案/结构 × 臧老师顶层设计 × OpenDesign按需增强 × image2视觉补给 × pptx-generation生成 × slide-studio微调
HTML/UI = GPT信息架构 × OpenDesign/设计系统按需增强 × impeccable审美规则 × 组件/样式系统 × Browser验证
```

每个工具只负责自己最强的一段：参谋本部路由，女娲在需要时组队，不另起重复入口。

### 输出原则

- 只要结果里包含“页面图/封面图/首页图/导出预览图”，就按图像产出快路径处理
- PPT不只是一份可编辑文件，必要时还要同步产出可直接预览的页面图片
- 需要用户看效果时，优先展示渲染图，再回传工程文件
- PPT/HTML交付默认只保留两个入口：预览 + “文件在……”

---

## 模块一：风格体系

| 风格 | 适用场景 | 色系 |
|------|---------|------|
| 数据看板 | 报表/KPI | 冷色调 |
| 学术 | 论文/研究报告 | 暖白 |
| 企业 | 商业计划/汇报 | 商务蓝 |
| 创意 | 提案/品牌 | 高饱和 |
| 医疗 | 医学/健康 | 浅绿 |
| 金融 | 投资/分析 | 深蓝金 |
| PPT通用 | 臧老师默认风格 | 自适应 |

---

## 模块二：图表体系

关系、分布、对比、时序、流程五类22种图表范式。数据密集页优先用图表、布局和强调色解决，不强行生图。

---

## 模块三：PPT五段融合工作流

### 融合策略

| 层次 | 工具 | 职责 | 调用时机 |
|------|------|------|---------|
| 内容结构 | GPT + content.md | 受众、叙事、页纲、讲稿、slide-spec.json | 立项阶段 |
| 顶层设计 | 臧老师(elite-powerpoint-designer) | 视觉风格、布局理念、色彩体系、排版规则 | 设计阶段 |
| 设计增强 | OpenDesign(按需) | 设计系统抽取、多方向探索、Deck/原型/HTML预览 | 需要更强视觉探索或交互预览时 |
| 视觉补给 | image2/imagegen + prompt_engine.md | 封面、章节图、图标风格图、场景图、产品视觉 | 素材阶段 |
| 技术引擎 | pptx-generation | 底层PPTX生成、XML操作、批量渲染 | 生成阶段 |
| 精细调控 | slide-studio | 模板应用、元素微调、动画、导出优化 | 微调阶段 |
| 审美验收 | qa.md + impeccable规则 | 可读性、信息密度、反AI味、品牌一致性 | 交付前 |

### 完整流水线

```
① GPT/content.md 重构逐字稿/资料并生成 slide-spec.json
   - 目标受众、演讲场景、核心信息
   - 每页一句话结论、证明/案例/练习、讲稿要点、是否需要配图
    ↓
② 臧老师(顶层设计) 定风格、配色、字体、版式、页型
   - 模板只作为视觉系统和版式库，不决定内容顺序
   - 按每页内容意图选择模板页型，禁止按模板页序硬塞逐字稿
    ↓
②.5 OpenDesign 按需做设计系统抽取、Deck视觉探索或HTML预览验证
   - 不替代PPTX主线，只增强设计判断和可视化预览
   - 动态/交互只在表达需要或用户要求时启用
    ↓
③ prompt_engine.md 为每页所需图片生成 image-spec.json
    ↓
④ image2 / imagegen 生成所需图像，保存到项目素材目录
    ↓
⑤ pptx-generation 生成PPTX文件
    ↓
⑥ slide-studio 微调模板、动画、插图、导出高清
    ↓
⑦ 每页独立渲染为高清图片并预览
    ↓
⑧ 需要视频时传入ComfyUI/Remotion做动效
```

### PPT交付底线

- 逐字稿先重构成页纲；每页必须有一个结论句
- 模板只按功能选用，不按原顺序填文字；像 Word 投影就退回
- OpenDesign 只做设计增强和预览验证，不作为每次必跑环节；动态效果按需启用
- 中文内容走 UTF-8 文件链路，导出后抽检 XML 或预览
- 不在每次 PPT 开头重复检查 unzip、运行时或模板检测工具；先生成，失败后再做针对性诊断并切备用链路

### 何时用哪个

| 场景 | 主用 | 辅助 |
|------|------|------|
| 从零做PPT | GPT内容+臧老师设计+pptx-gen生成 | image2补图+slide-studio微调 |
| 改模板PPT | slide-studio | pptx-gen批量改 |
| 批量生成 | pptx-generation | slide-studio样式 |
| Markdown/文档转PPT | content.md结构化 | Marp仅作快速草稿链路 |
| HTML转PPT | 默认不启用html2pptx | 用户明确要求时临时评估 |
| PPT转视频 | 全流水线 | Remotion/ComfyUI |
| Deck/原型预览增强 | OpenDesign | Browser/Presentations |

### PPT图片自动生成规则

- 每页先判断是否真的需要图像：能用版式、图表、图标解决的，不强行生图
- 需要图像时，由 GPT 先把页面意图扩写成 `image-spec.json`，再交 image2/imagegen
- 含文字的图片默认避免直接让模型生成文字；文字放在PPT/HTML层，图片只负责视觉
- 生成图像必须匹配整套PPT风格，包括色调、构图、留白和画幅
- 所有生成图像写入项目素材目录，不只留在临时目录

---

## 模块四：HTML/UI制作美化

### 融合策略

```
HTML/UI制作 = GPT信息架构 × impeccable审美规则 × 组件/样式系统 × Browser验证
```

### 工作流

```
① GPT/content.md 提炼页面信息架构
   - 用户是谁、主路径是什么、首屏要传达什么
    ↓
② impeccable 检查布局、色彩、字体、动效、反AI味
    ↓
③ 组件/样式系统 复用项目已有组件；无组件时再参考开源组件生态
    ↓
④ 视觉部风格体系 确保风格统一
    ↓
⑤ 生成HTML/CSS/JS或前端组件
    ↓
⑥ Browser打开本地页面验证响应式、截图、可用性
    ↓
⑦ impeccable复审并修正
```

### impeccable 六大规则

1. 视觉层级清晰：先结构后装饰
2. 色彩系统一致：主色、辅色、中性色各有职责
3. 间距有节奏：避免全页面同一种padding
4. 字体不超过2种，层级用字号和字重拉开
5. 响应式优先，移动端不能只是缩小桌面端
6. 动效有目的，不为动而动

### HTML外部组件吸收原则

- Tailwind、shadcn等只作为组件与token参考，不覆盖项目既有设计系统
- daisyUI默认丢弃，不进入主链路；只有低保真原型才可临时参考
- 先用项目原生栈实现，除非用户明确要求引入依赖
- 禁止模板味：不得生成同质化卡片网格、无意义渐变、默认SaaS首页套路
- 重要页面必须用 Browser 做截图或视觉验证

### 软件/网页UI硬门禁

- 先确定界面场景：谁在什么环境下使用，任务压力是什么
- 不默认深色，也不默认白底；主题必须服务场景
- 不允许所有页面都是卡片网格
- 不允许默认字体栈糊弄；必须有明确字体层级
- 不允许只做首屏，忽略空状态、错误态、加载态
- 不允许只看桌面端，必须检查移动端或窄屏
- 不允许交付前没有 Browser 截图/预览验证
- 丑、乱、像模板，直接退回重做

---

## 模块五：漫画/知识图

漫画融合策略：

```
comicmaster(整体流程) × flexicomic(灵活布局) × baoyu(风格体系) × image2视觉补给
```

---

## 模块六：反模式清单

- 禁止AI塑料渐变
- 禁止在生图上修补文字
- 禁止纯黑白默认风
- 禁止无意义动画
- 禁止颜色超过3种主色
- 禁止字体超过2种
- 禁止无留白的密集排版
- 禁止所有页面都长得像同一个模板

---

## 与各部门协作

- 与content.md协作：接收 `slide-spec.json` 作为PPT输入
- 与prompt_engine.md协作：生成 `image-spec.json` 和 `html-design-spec.json`
- 与comfyui.md协作：PPT图片传入ComfyUI做动效或高质量图像补足
- 与qa.md协作：设计稿交白帽纠察和审美验收
- 与proving_ground.md协作：PptxGenJS、Marp、reveal.js、shadcn按条件试验；html2pptx、daisyUI默认丢弃

## 模块七：新增领域专精专家（女娲统一调度）

| 专家 | 专长 | 所属师团 |
|------|------|---------|
| Brand Guardian | 品牌视觉一致性 | 第二师 |
| Visual Storyteller | 视觉叙事、信息图 | 第二师 |
| Whimsy Injector | 创意趣味、记忆点 | 第二师 |
| UI Designer | 界面设计、组件系统 | 第二师 |
| UX Architect | 用户体验、信息架构 | 第二师 |
| Image Prompt Engineer | 图像prompt | 第二师+第三师 |

---

## 八、Codex插件融合

| 插件/skill | 用途 | 调用方式 |
|-------------|------|---------|
| Presentations | PPT生成、修改、预览 | PPT默认可用能力 |
| Browser | HTML/UI验证、截图 | 本地页面必须优先验证 |
| Figma | 设计稿转代码、UI组件库 | 需要UI实现时优先用Figma plugin |
| Canva | 轻量设计、PPT素材 | 快速素材补充 |
| Remotion | React视频、动态图形 | 需要程序化动画时用Remotion |
| Cloudinary | 媒体资产CDN管理 | 大量图片管理时调用 |

**融合原则：** GPT定内容，臧老师定审美，image2补视觉，PPTX/HTML负责可编辑产物，Browser/QA负责验收。
