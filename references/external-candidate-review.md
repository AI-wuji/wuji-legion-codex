# External Candidate Review

Status: source review complete for the listed candidates, 2026-07-13. This document separates evidence from prior README-level impressions. A candidate is not fused unless its retained slice is callable and behavior-verified.

| Candidate | Source evidence inspected | Current conclusion |
| --- | --- | --- |
| [addyosmani/agent-skills](https://github.com/addyosmani/agent-skills) | Local checkout: MIT `LICENSE`; `scripts/validate-skills.js`; debugging, review, spec-driven, and source-driven Skill bodies; plugin/host command layouts. | Distill, do not install wholesale. The structured failure protocol, two-axis review, source-first preflight, bounded change capsule, and validation discipline are now executable in the router/CLI. Its host hooks, resident workflow commands, and mandatory multi-file planning conflict with Aji-only merge and small tasks. This is doctrine/code-path distillation, not a copied package or a claimed fused upstream capability. |
| [DeusData/codebase-memory-mcp](https://github.com/DeusData/codebase-memory-mcp) | Bundled v0.9.0 binary `--help`, CLI tool list, MIT `LICENSE`, and `install.ps1`. A direct `cli index_repository` probe started its supervisor but the contained indexing worker exited non-zero. | Keep as a cold, optional external index only. Do not invoke `install`, do not add persistent host configuration, and do not claim a successful index or fusion. The native bounded workspace graph remains primary until an isolated real index/search probe succeeds. |
| [Fission-AI/OpenSpec](https://github.com/Fission-AI/OpenSpec) | GitHub source review: MIT; `package.json` / `bin/openspec.js`; commander entrypoint in `src/cli/index.ts`; strict, JSON-capable validation in `src/commands/validate.ts`; filesystem workflow creation in `src/commands/workflow/new-change.ts`; Vitest validation tests for concurrency, CRLF, ambiguous items, and proposal-less changes. | Distill only the machine-readable change-contract gate. `wuji change-capsule --strict` now validates scope, acceptance, verification, and rollback with structured evidence and non-zero failure. Reject its initializer, change directory tree, archive/store, telemetry, and cross-tool instructions: they conflict with one small task and Aji-only merge. This is a verified local implementation, not copied OpenSpec or a fused upstream package. |
| [mattpocock/skills](https://github.com/mattpocock/skills) | GitHub source review: MIT; `package.json`; destructive global symlink installer `scripts/link-skills.sh`; engineering Skills for diagnosing bugs, code review, research, and specification; source-first/reproducible-feedback-loop guidance. | Do not run the installer or adopt the catalog. Its source-first research and review separation overlap existing routing. The evidence-first bug loop is retained through the active worker failure protocol; this is a local distilled rule, not package fusion. |
| [firecrawl/firecrawl](https://github.com/firecrawl/firecrawl) and [firecrawl/cli](https://github.com/firecrawl/cli) | Main service is AGPL-3.0 and contains a large self-hosted API/worker stack. CLI source: TypeScript entrypoint `src/index.ts`, scrape/search can use keyless tier, authenticated commands persist credentials under the user profile; tests cover credential priority and permissions. | Do not copy, self-host, configure, or default-select it. It remains a deferred optional external read-only provider until a real adapter and comparative behavior probe demonstrate value without storing credentials. No manifest-only “integration” is claimed. |
| [obra/superpowers](https://github.com/obra/superpowers) | GitHub source review: MIT; Codex plugin manifest; Claude session hook; systematic-debugging and verification-before-completion Skills; packaging and explicit-skill test scripts. | Distill the compatible bounded failure discipline into executable worker contracts: evidence before fixes, stop after two disproven hypotheses for architecture reassessment, and fresh regression verification. Reject hooks, forced worktrees, resident skills, and plan workflow. This is tested local contract behavior, not a fused upstream package. |
| [DietrichGebert/ponytail](https://github.com/DietrichGebert/ponytail) @ `14a0d79548d4de8fc2de95c1b94bb0de63a739d3` | Complete MIT snapshot retained at `wuji-capability-sources/upstream-snapshots/ponytail`; canonical `AGENTS.md`, `skills/ponytail*`, lifecycle hooks, package configuration, and 108 upstream tests inspected. | Distilled into local code-route/worker-contract behavior: trace flow with anchors; choose the first valid rung from YAGNI through local reuse, stdlib/platform/dependency, to minimum code; inspect callers and fix shared root causes; keep the smallest proportional check. Route JSON, serialized contracts, and the local behavior probe verify this local rule, not end-to-end native-child execution. The snapshot stays cold. Hooks, persistent modes, MCP, platform plugins, personality, and standalone commands are rejected because they would create a second control plane or override user scope. |
| [guodongwang370-stack/dashiai-ppt](https://github.com/guodongwang370-stack/dashiai-ppt) @ `05f540021b470c51316a90737970196760a0e05e` | Source tree review: a Web/PPT runtime with `project/scripts/layout-query.mjs`, `inspect-layout.mjs`, `validate-goal-spec.mjs`, `validate-swiss-deck.mjs`, `export-pptx.mjs`, React theme metadata and bundled media. `project/package.json` exposes validation/export commands but no test command; the tree has no test files and the repository declares no license. | Do not copy its code, templates, or media into this repository. The local `wuji-dashiai-deck-adapter` is a behavior-verified, narrow automatic adapter for explicit editable/template HTML-deck tasks; `scripts/verify-dashiai-ppt.ps1` validates its local generation and layout route. It is not a copy or fusion of the upstream package, and complete upstream DashiAI remains cold because the license does not permit reusable inclusion. |

Required evaluation sequence: inspect source entrypoints, executable scripts/configuration, tests or behavior probes, and license; map overlap and conflict with single-Aji/small-task boundaries; then retain the smallest compatible callable slice and run a behavior comparison. README descriptions are navigation only.
